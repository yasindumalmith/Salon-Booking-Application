apiVersion: v1
kind: Service
metadata:
  name: salon-service
spec:
  selector:
    app: salon-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8081
  type: ClusterIP