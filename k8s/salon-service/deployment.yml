apiVersion: apps/v1
kind: Deployment
metadata:
  name: salon-service
  labels:
    app: salon-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: salon-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: salon-service
    spec:
      nodeSelector:
        node-role: salon-worker
      containers:
        - name: salon-service
          image: yasindumalmith/salon-service:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 8081

          envFrom:
            - configMapRef:
                name: salon-service-config
            - secretRef:
                name: salon-service-secret

          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 15